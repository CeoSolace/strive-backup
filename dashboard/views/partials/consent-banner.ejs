<div id="consent-banner"
  class="fixed bottom-4 left-1/2 -translate-x-1/2 z-50 w-[95%] max-w-2xl rounded-2xl border border-zinc-200 bg-white p-4 shadow-soft dark:border-zinc-800 dark:bg-zinc-900 hidden"
>
  <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
    
    <div class="text-sm text-zinc-700 dark:text-zinc-300">
      We use cookies for analytics and improving the dashboard.
      You can accept or decline.
    </div>

    <div class="flex gap-2">
      <button id="consent-decline" class="btn btn-secondary">
        Decline
      </button>
      <button id="consent-accept" class="btn btn-primary">
        Accept
      </button>
    </div>

  </div>
</div>
